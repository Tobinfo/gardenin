{
  "name": "gardensnap-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Laptop-camera prototype and iOS scaffold for GardenSnap.",
  "scripts": {
    "start": "node scripts/serve-prototype.js",
    "check": "node --check prototype/app.js && node --check scripts/serve-prototype.js && node --check scripts/plant-id-provider.js && node --check scripts/perenual-provider.js"
  }
}
